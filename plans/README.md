# Implementation Plans

Generated by the improve skill. Execute in the order below unless dependencies
say otherwise. Each executor must read the selected plan fully before starting,
honor its STOP conditions, and update the status row when done.

## Current deep audit

- Audit run: 2026-06-23
- Planned at commit: `fc9ae05`
- Scope: full tracked repo, all packages, all nine improve categories.
- Verification observed:
  - `pnpm check:versions` passed.
  - `pnpm lint` passed.
  - `pnpm typecheck` passed.
  - `pnpm test` passed: API 51 tests, web 5 tests.
  - `pnpm --filter web test:e2e` passed: 6 tests.
  - `cd apps/api && uv run python scripts/evaluate_extraction.py` reported `exact_row=27/27` and `false_merge=3/3`.
  - `pnpm audit --audit-level high --prod` passed; `pnpm audit --prod` reports one moderate PostCSS advisory through Next's transitive dependency.
  - `cd apps/api && uv run pip-audit` could not run because `pip-audit` is not installed.
- Subagents: not used. The host multi-agent tool only permits spawning after explicit user authorization for subagents; this audit was performed directly.

## Active execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 007 | Make replay upload update the workstation and honor replay row failures | P1 | M | — | TODO |
| 008 | Add a minimal CI verification baseline | P1 | M | — | TODO |
| 009 | Add runtime validation at WebSocket and frontend event boundaries | P2 | M | 007 recommended | TODO |
| 010 | Split the workstation page into focused components and hooks | P2 | L | 007 and 009 recommended | TODO |
| 011 | Replace public README placeholders with a concise portfolio quickstart | P2 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- 009 can run before 007, but it is cleaner after 007 because replay upload will add another frontend event ingestion path.
- 010 should run after behavior changes from 007 and boundary hardening from 009 to avoid refactoring a moving target.
- 008 and 011 are independent.
- Historical: 005 depends on 002 because chaotic fixtures and simulator chaos controls extend the evaluator.
- Historical: 006 depends on 005 because bounded fuzzy matching needs hard-negative false-merge metrics first.

## Completed plan history

- 001: deterministic ticker canonicalization - merged in PR #2.
- 002: extraction evaluation fixtures and metrics - merged in PR #2.
- 003: fast fuzzy and LLM extraction fallback design - merged in PR #2.
- 004: backend-owned extraction provider config design - merged in PR #2.
- 005: chaotic extraction evaluation and simulator controls - done on `main`.
- 006: bounded fuzzy ticker resolver - done on `main`.

Completed plan files 001-004 were removed after their durable decisions landed
in `docs/`, `tasks/`, and current source/tests. Use git history or PR #2 for
their full original handoff content.

## Vetted findings from 2026-06-23 audit

| # | Finding | Category | Impact | Effort | Risk | Evidence |
|---|---------|----------|--------|--------|------|----------|
| 1 | Replay upload returns events but the browser ignores them | bug, tests | Replay can report success without updating chat, parsed events, or book state; this contradicts the product purpose. | M | MED | `apps/web/app/page.tsx:149-156` ignores response JSON; `apps/api/src/otc_to_book/api/main.py:25-47` returns replay events; `docs/PRODUCT.md:13` requires replay inspection. |
| 2 | Malformed replay rows can abort the whole replay | bug | `row["text"]` can raise instead of emitting row-level rejection when possible; this violates replay rules. | M | MED | `apps/api/src/otc_to_book/api/main.py:33-47`; `docs/architecture.md:103-106`. |
| 3 | No tracked CI protects main and root test omits E2E | dx, tests | Direct pushes can bypass Playwright and extraction metrics, weakening portfolio trust. | M | LOW | no `.github` workflow; `package.json:6-12` has no E2E/verify script; `apps/web/package.json:9` keeps E2E separate. |
| 4 | Runtime event boundaries are loosely validated | correctness, security | Malformed WS/server events can crash or mutate state through casts instead of producing diagnosable failures. | M | MED | `apps/api/src/otc_to_book/api/main.py:69-103`; `apps/api/src/otc_to_book/domain/models.py:178-186`; `apps/web/app/page.tsx:85-87`; `apps/web/lib/state.ts:77-98`. |
| 5 | Main workstation route is a high-churn monolith | tech-debt, architecture | Future UI work must edit one 871-line route that owns stream, upload, layout, and rendering concerns. | L | MED | `apps/web/app/page.tsx` has 871 lines and 19 changes in the last 30 commits. |
| 6 | Public README is still placeholders | docs | A portfolio reviewer sees TODOs before the working architecture/docs. | S | LOW | `README.md:5-19`; `AGENTS.md` README ownership rule. |
| 7 | Dependency audit posture is partial | security, dx | JS high/critical audit gate passes, but moderate PostCSS advisory exists and Python audit tooling is absent. | S/M | LOW | `pnpm audit --prod`; `cd apps/api && uv run pip-audit` fails because `pip-audit` is missing. |

## Direction options

- Promote replay from "implemented but not covered" to a first-class demo path after plan 007. This is grounded in `docs/PRODUCT.md:13`, the API replay endpoint, and the current UI upload affordance.
- Keep future LLM/provider work backend-owned and gated by extraction metrics. This is already documented in `docs/extraction-strategy.md` and should not be implemented until the current deterministic demo path is airtight.
- Consider a small generated or shared contract package only after plan 009 proves hand-written guards are becoming noisy. Current repo size does not justify it yet.

## Historical findings

- Accepted ticker typos fragment the book. Addressed by completed plans 005 and 006 through session-scoped valid ticker pool, explicit aliases, and bounded fuzzy matching.
- The repo had no extraction quality metric harness. Addressed by completed extractor evaluation plans.
- The simulator was too clean for fuzzy extraction work. Addressed by chaotic fixture and simulator controls.

## Implementation notes

- Plan 005 added chaotic positive, hard-negative, and rejection fixture categories plus simulator chaos controls.
- Plan 006 enabled bounded fuzzy matching against the session valid ticker pool only.
- `PETOR27` merges to `PETRO27` only after `PETRO27` exists in the session pool.
- `PETR27` and `BOVE26` are fuzzy exclusions; `BOVE26` must not merge into `BOVA26` before explicit acceptance.
- Non-Petrobras fuzzy-positive fixtures are explicitly deferred until a safe accepted pair is chosen; this branch still covers non-Petrobras chaos and hard negatives with `VALE29`, `BOVA26`, and `BOVE26`.
- Expanded evaluator output reports `exact_row=27/27` and `false_merge=3/3` on the current fixture set.

## Findings considered and rejected

- Full shared/generated contract package now: rejected for this repo size. Plan 009 starts with small runtime guards and can revisit generation later.
- Add persistence or an instrument master: rejected because V1 docs explicitly keep state in memory and defer a persistent master until session-pool behavior is measured.
- Immediate LLM fallback implementation: rejected because deterministic extraction and evaluation are the current portfolio story; provider profiles are roadmap work.
- Treat the moderate PostCSS advisory as P1: rejected because high/critical JS audit passes and the advisory is transitive through Next; track it under CI/dependency posture instead.
- General fuzzy matching directly in V1 extraction: rejected for now because it can merge real instruments incorrectly without an instrument universe and evaluation harness.
- Full instrument master in this iteration: rejected because V1 docs say not to add a full instrument master, and an alias map is enough to address the observed issue.
- Persistent valid ticker pool: rejected for V1 because the current backend is intentionally in-memory.
- LLM fallback as immediate runtime behavior: rejected until plan 002 establishes quality metrics and plan 003 defines latency, confidence, and fallback gates.
- Dashboard-side extraction: rejected because API keys, provider URLs, timeout policy, validation, and book updates must remain backend-owned.
- Unmeasured fuzzy matching: rejected because wrong ticker merges corrupt the book more severely than missed corrections.
